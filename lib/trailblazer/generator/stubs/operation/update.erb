module <%= namespace %>
  class <%= name %> < Trailblazer::Operation
    class Present < Trailblazer::Operation
      step Model(<%= concept %>, :find_by)
      step Contract::Build(constant: <%= concept %>::Contract::Create)
    end

    step Nested(Present)
    step Contract::Validate()
    step Contract::Persist()
  end
end
